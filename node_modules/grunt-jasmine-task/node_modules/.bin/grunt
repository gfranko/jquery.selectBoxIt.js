#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../grunt/bin/grunt" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../grunt/bin/grunt" "$@"
  ret=$?
fi
exit $ret
