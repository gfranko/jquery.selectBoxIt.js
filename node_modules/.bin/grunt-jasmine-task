#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../grunt-jasmine-task/bin/grunt-jasmine-task" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../grunt-jasmine-task/bin/grunt-jasmine-task" "$@"
  ret=$?
fi
exit $ret
